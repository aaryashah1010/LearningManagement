ENV=development
PORT=8000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=learning_management

JWT_SECRET=change-me
JWT_REFRESH_SECRET=change-me-too
JWT_AUTH_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=30

DEFAULT_STUDENT_PASSWORD=change-me

# Not a real decision yet — "anthropic" here is a placeholder to unblock local dev,
# not a confirmed choice. Pick for real before this goes anywhere beyond a laptop.
LLM_PROVIDER=anthropic
LLM_API_KEY=

S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
